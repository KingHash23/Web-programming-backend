const welcome = (name) => {
    return `Hi ${name}. You are welcome to the system 😊🌍🦍`;
}
  
  // Exporting the function as a module for use in other files.
  module.exports = {
    welcome
}

